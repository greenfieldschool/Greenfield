# Greenfield School Website

## Local development
1. Copy `.env.example` to `.env.local` and fill in values (optional for initial UI work).
2. Install dependencies:
   - `npm install`
3. Run the dev server:
   - `npm run dev`

## Deploy
Deploy on Vercel. Set environment variables in the Vercel project settings.
